default:
  autoload:
    '': %paths.base%/features/bootstrap
  suites:
    default:
      paths:
        - %paths.base%/features
      context:
        - %paths.base%/bootstrap/FeatureContext:
          parameters:
            wd_host: http://127.0.0.1:4444/wd/hub
            wd_capabilities: { "browser": "firefox", "version": "14"}

